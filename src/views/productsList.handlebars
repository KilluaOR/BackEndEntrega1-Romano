<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <h2>Productos</h2>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/">Inicio</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/realTimeProducts">Lista de productos en
            tiempo real</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/productsList">Productos</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="card">
  <div class="card-header">
    <h5 class="mb-0">Lista de Productos</h5>
  </div>
  <div class="card-body">
    <ol id="productList" class="list-group list-group-numbered">
      {{#each products}}
        <li
          id="p-{{this.id}}"
          class="list-group-item d-flex justify-content-between align-items-center"
        >
          <span><strong>{{this.title}}</strong></span>
          <span class="badge bg-primary">${{this.price}}</span>
        </li>
      {{/each}}
    </ol>
  </div>
</div>